import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: "standalone", // This option is used to build the application as a standalone server
};

export default nextConfig;
